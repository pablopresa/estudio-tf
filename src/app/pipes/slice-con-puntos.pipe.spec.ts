import { SliceConPuntosPipe } from './slice-con-puntos.pipe';

describe('SliceConPuntosPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceConPuntosPipe();
    expect(pipe).toBeTruthy();
  });
});
